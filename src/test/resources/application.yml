spring:
  flyway:
    locations: classpath:db/test-migration
  datasource:
    url: jdbc:postgresql://localhost/p2p_test
    username: postgres
    password: softeni0926

#mybatis:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.example.p2p.mapper: DEBUG