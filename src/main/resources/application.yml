spring:
  flyway:
    locations: "classpath:db/migration"
    # passwordを別の場所に保存
  datasource:
    url: "jdbc:postgresql://localhost/p2p"
    username: "postgres"
    password: "softeni0926"
mybatis:
  configuration:
    map-underscore-to-camel-case: true

