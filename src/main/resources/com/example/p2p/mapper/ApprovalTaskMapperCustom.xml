<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.p2p.mapper.ApprovalTaskMapperCustom">
  <insert id="bulkInsert">
    insert into approval_task
    (
       document_id,
       step_order,
       user_id,
       document_type,
       status,
       comment,
       step_name
    ) 
    values
    <foreach collection="tasks" item="task" separator=",">
     (
      #{task.documentId},
      #{task.stepOrder},
      #{task.userId},
      #{task.documentType},
      #{task.status},
      #{task.comment},
      #{task.stepName}
      )
    </foreach>
  </insert>
  
  <resultMap id="approvalProgressMap" type="com.example.p2p.dto.app.ApprovalProgressStepDto">
    <id property="stepOrder" column="step_order"></id>
    <result property="stepName" column="step_name"></result>
    <collection property="approvalProgressApprovers" ofType="com.example.p2p.dto.app.ApprovalProgressApproverDto">
      <id property="userId" column="user_id"></id>
        <result property="userName" column="user_name"></result>
        <result property="status" column="status"></result>
        <result property="comment" column="comment"></result>
        <result property="actedAt" column="acted_at"></result>
    </collection>
  </resultMap>
  
  <select id="selectApprovalProgressSteps" resultMap="approvalProgressMap">
    select 
      apt.step_order,
      apt.step_name,
      u.user_id,
      CONCAT_WS(' ', u.last_name, u.first_name) as user_name,
      apt.status,
      apt.comment,
      cast(apt.acted_at as date) as acted_at
    from approval_task apt
    join users u
      on apt.user_id = u.user_id
    where apt.document_id = #{documentId}
  </select>
</mapper>
