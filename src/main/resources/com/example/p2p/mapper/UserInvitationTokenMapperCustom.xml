<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.p2p.mapper.UserInvitationTokenMapperCustom">
  <select id="selectUserNameAndEmail" resultType="com.example.p2p.dto.InitialPasswordSetupViewDto">
    select 
      u.last_name,
      u.first_name,
      u.email
    from user_invitation_token uit
    join users u
      on uit.user_id = u.user_id
    where uit.token_hash = #{tokenHash}

  </select>
</mapper>
